-- Create Users Table
CREATE TABLE IF NOT EXISTS Users (
    user_id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT NOT NULL UNIQUE,
    email TEXT NOT NULL UNIQUE,
    password TEXT NOT NULL,
    zip_code TEXT NOT NULL
);

-- Create User Sessions Table
CREATE TABLE IF NOT EXISTS UserSessions (
    session_id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    token TEXT NOT NULL,
    FOREIGN KEY (user_id) REFERENCES Users(user_id)
);

-- Create User Settings Table (Optional)
CREATE TABLE IF NOT EXISTS UserSettings (
    user_id INTEGER PRIMARY KEY,
    -- Add additional columns for user settings/preferences
    FOREIGN KEY (user_id) REFERENCES Users(user_id)
);

-- Create Treatment Options Table
CREATE TABLE IF NOT EXISTS TreatmentOptions (
    treatment_id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    address TEXT NOT NULL,
    -- Add other relevant columns
);

-- Create Mental Health Professionals Table
CREATE TABLE IF NOT EXISTS MentalHealthProfessionals (
    professional_id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    specialization TEXT,
    address TEXT NOT NULL,
    -- Add other relevant columns
);
